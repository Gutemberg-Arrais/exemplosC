#include <stdio.h>
int main(int argc, char const **argv)
{
    // char c[6];
    // c[0] = 'T';
    // *(c + 1) = 'u';
    // c[2] = 114;
    // *(c + 3) = 109;
    // c[4] = 'a';
    // c[5] = '\0';

    char *c = "Turma de Estrutura de dados";

    printf("Texto em string: %s", c);
    /* code */
    return 0;
}
